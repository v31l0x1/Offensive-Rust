[package]
name = "HellsGate"
version = "0.1.0"
edition = "2024"

[dependencies]
ntapi = {version = "0.4.3"}
winapi = {version = "0.3.9", features = [
    "processthreadsapi",
]}

[build-dependencies]
nasm-rs = { version = "0.3.2" }
